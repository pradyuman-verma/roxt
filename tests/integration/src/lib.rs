//! Intentionally empty. This package exists so end-to-end tests can live at
//! `tests/integration` per the enforced repository layout while remaining a
//! normal cargo workspace member; everything of substance is under `tests/`.
